@foreach($dataa as $product)

    {{ $product['product'] }}
    {{ $product['quantity'] }}

@endforeach
@foreach($dataa as $product)
        {{ $product['product'] }}

        {{ $product['quantity'] }}

    @endforeach